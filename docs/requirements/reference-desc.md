阅读 `/reference/src/server` 的代码，学习面向对象的思路，并最终生成一个 skills，抽象出一套通用的桌面游戏代码实现思路。关注的是游戏架构抽象思路，而不是具体业务实现，例如：
    1. `game` ，用于控制每一局游戏的进程
    2. `player` 类，控制每个玩家的状态和交互
    3. `action`/`input` 类，游戏内的操作，需要以队列的形式进行交互。交互分为系统的默认操作，以及需要用户输入的 prompt 类操作。
    4. `cards` 游戏核心卡牌，通过父类和子类进行管理，并实现卡牌的注册
    5. 业务逻辑，比如 expension，如何集成。这一步关注的是集成思路，而不是具体的业务实现。

不需要关注后端的一些框架实现，比如鉴权、db、序列化。只关注游戏性部分。
生成 skills ，总结可参考思路，对于核心文件，生成一个 references 文件夹，里面逐一说明对应文件的核心思路。