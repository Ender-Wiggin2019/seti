生成如下新的 package：
- server。这一层是游戏核心层，会引入 common 的包作为来源。
- client。这一层是游戏前端层，引入 common 和 cards。用于游戏基本交互，以及登陆注册/创建房间等功能

目前优先关注 server 层的实现。具体要求为：

### 请求层

1、技术选型。选择你最擅长并最合适用于本项目的node后端技术栈，例如 express, nestjs。
2、请求方式。restful或者websocket，你理解游戏需求后，给出方案推荐。

### DB 层。
实现 dto, 序列化与反序列化，需要支持 undo 操作。推荐使用 drizzle orm。

### 内存游戏层。

这一层需要：
1、阅读 `rule.md` 理解游戏规则，明白最终目标
2、参考 `.agents/skills/board-game-architecture` 的说明 (这里面是另一个项目的架构设计)，参考实现，考虑一下这个项目如何实现类似的引擎架构

### 测试
单测支持，要求覆盖全流程，每个文件都需要对应一个单测。选择合适的单测框架。

### 其余部分

分析一下作为工程化还有什么缺漏的架构或者细节，列举出来。

---

最终生成一份 arch-server.md，说明 server 层的架构。其中一些需要确认的选项，和用户确认。