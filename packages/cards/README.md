# @ender-seti/cards

卡牌组件与静态资源包，供 `web` 与 `client` 复用。

## 运行方式

该包不是独立服务，不需要 `dev server`。

在包目录执行：

```bash
pnpm build
```

## 常用命令

```bash
pnpm clean
pnpm build
pnpm lint
pnpm typecheck
```

## 产物内容

- 组件库入口：`dist/index.*`
- 样式：`dist/styles/card.css`
- 资源：`dist/assets/images`
