/**
 * Registers custom DESC effect handlers on the default BehaviorExecutor.
 *
 * New card-specific logic should live in an individual card class instead of
 * this global registry.
 */

export function registerDescHandlers(): void {
  // All migrated DESC effects currently live on dedicated card classes.
}
